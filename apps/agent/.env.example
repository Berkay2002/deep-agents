# Google Gemini API Configuration
GOOGLE_API_KEY=your_google_api_key_here

# Optional: Override default model
# GOOGLE_GENAI_MODEL=gemini-2.5-pro

# Optional: Tavily search integration
# TAVILY_API_KEY=your_tavily_api_key_here

# MCP Servers
# Note: MCP servers are currently disabled due to compatibility issues
# Most official Anthropic MCP servers are Python-based (require uvx/pip)
# Node.js alternatives need proper investigation and testing
#
# To enable MCP servers:
# 1. For Python-based servers: Install Python and use uvx
# 2. For Node.js servers: Find packages with proper bin configuration
# 3. Update apps/agent/src/utils/tools.ts mcpServers configuration

# Optional: Custom agent instructions
# DEEP_AGENT_INSTRUCTIONS="Custom instructions for the Deep Agent"

# Optional: LangGraph server token
# LANGGRAPH_SERVER_TOKEN=your_server_token_here
