# Google Gemini API Configuration
GOOGLE_API_KEY=your_google_api_key_here

# Optional: Override default model
# GOOGLE_GENAI_MODEL=gemini-2.5-flash

# Optional: Tavily search integration
# TAVILY_API_KEY=your_tavily_api_key_here

# MCP Servers
# Sequential Thinking and DeepWiki MCP servers are enabled by default (no configuration needed)
# These public servers provide structured reasoning and knowledge base access

# Optional: GitHub Copilot MCP (requires GitHub PAT)
# GITHUB_PAT=ghp_your_github_personal_access_token
# Note: Only set this in development. Leave empty in production for security.
# Required scopes: repo, read:org
# Get a token at: https://github.com/settings/tokens/new

# Optional: Custom agent instructions
# DEEP_AGENT_INSTRUCTIONS="Custom instructions for the Deep Agent"

# LangGraph custom auth configuration (must align with web proxy)
LANGGRAPH_AUTH_SECRET=replace-me-with-a-strong-secret
LANGGRAPH_AUTH_ISSUER=http://localhost:3000  # Use your domain in production (e.g., https://yourdomain.vercel.app)
LANGGRAPH_AUTH_AUDIENCE=deep-agents-langgraph

# Optional: LangGraph server token (legacy support)
# LANGGRAPH_SERVER_TOKEN=your_server_token_here